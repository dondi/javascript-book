2 + 2;                // Adds two and two, but ignores the result.
"Hello";              // Also legal but totally useless.
Math.sqrt(100);       // Does a computation, but ignores the result.
"a".toLowerCase();    // Computes a new string but ignores it.
