// UGLY CODE - DO NOT DO THIS
capital =
      (state === "Baden-Württemberg") ? "Stuttgart"
    : (state === "Bayern") ? "Munchen"
    : (state === "Berlin") ? "Berlin",
    : (state === "Brandenburg") ? "Potsdam",
    : (state === "Bremen") ? "Bremen",
    : (state === "Hamburg") ? "Hamburg",
    : (state === "Hessen") ? "Wiesbaden",
    : (state === "Mecklenburg-Vorpommern") ? "Schwerin",
    : (state === "Niedersachsen") ? "Hannover",
    : (state === "Nordrhein-Westfalen") ? "Düsseldorf",
    : (state === "Rheinland-Pfalz") ? "Mainz",
    : (state === "Saarland") ? "Saarbrücken",
    : (state === "Sachsen") ? "Dresden",
    : (state === "Sachsen-Anhalt") ? "Magdeburg",
    : (state === "Schleswig-Holstein") ? "Kiel",
    : (state === "Thüringen") ? "Erfurt"
    : undefined;
