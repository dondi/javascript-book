var dog = {};              // An object with no properties.
dog.name = "Kärl";         // Now the object has one property.
dog.breed = "Rottweiler";  // Now the object has two properties.
delete dog.name;           // Now one property again.
