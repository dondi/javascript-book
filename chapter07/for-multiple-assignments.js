// It is possible to do multiple assignments in the first section
// of a for-statement.  Each will be done only once.  This means
// the test to exit the loop can be done with a local variable.
for (var i = 0, n = a.length; i < n; i += 1)
